# CI_CD_Pipeline

An automated pipeline that runs tests &amp; lint on PRs, deploys to staging on merge, and deploys to production via manual approval. Tech: Python (Django) backend, React frontend, Docker, GitHub Actions.
